apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: intel-plugins-gpu
  namespace: flux-system
spec:
  dependsOn: 
    - name: intel-plugins-nfd
  interval: 10m0s
  path: ./deployments/gpu_plugin/overlays/nfd_labeled_nodes
  prune: true
  sourceRef:
    kind: GitRepository
    name: intel-plugins
