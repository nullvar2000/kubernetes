apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfd
  namespace: node-feature-discovery
spec:
  interval: 1h
  releaseName: nfd
  chart:
    spec:
      chart: node-feature-discovery
      version: 'v0.19.*'
      sourceRef:
        kind: HelmRepository
        name: nfd
  values:
    prometheus:
      enable: true
