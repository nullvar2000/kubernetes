apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: kyverno-cleanup-clusterrole-patch
  annotations:
    policies.kyverno.io/title: Kyverno Cleanup Controller RBAC Patch
    policies.kyverno.io/description: >-
      Removes 'secrets' verb access from the Kyverno cleanup controller ClusterRole.
      This limits the blast radius if the Kyverno pod is compromised.
spec:
  background: false
  rules:
    - name: remove-secrets-access
      match:
        any:
          - resources:
              kinds:
                - ClusterRole
              names:
                - kyverno-cleanup-controller
      mutate:
        patchStrategicMerge:
          metadata:
            name: kyverno-cleanup-controller
          rules:
            - resources:
                - "*/*"
                - "-secrets"
