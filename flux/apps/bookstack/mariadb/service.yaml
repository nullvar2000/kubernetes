apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: bookstack
spec:
  selector:
    app: mariadb
  ports:
    - name: mariab
      port: 3306
      targetPort: 3306
      protocol: TCP
  type: ClusterIP
