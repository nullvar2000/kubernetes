apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: heimdall
  namespace: heimdall
spec:
  parentRefs:
  - name: gateway-api
    namespace: istio-gateway
  hostnames:
    - heimdall.${GLOBAL_DOMAIN}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: heimdall
      port: 80
