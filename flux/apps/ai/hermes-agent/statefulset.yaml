apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hermes-agent
  namespace: hermes-agent
  labels:
    app: hermes-agent
spec:
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  podManagementPolicy: OrderedReady
  selector:
    matchLabels:
      app: hermes-agent
  replicas: 1
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        app: hermes-agent
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: agent
          image: docker.io/nousresearch/hermes-agent:v2026.5.7 # {"$imagepolicy": "flux-system:hermes-agent-policy"}
          command:
            - hermes
          args:
            - gateway
            - run
          env:
            - name: HOME
              value: /opt/data
            - name: PATH
              value: /opt/hermes/.venv/bin:/opt/data/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
          ports:
            - name: hermes-agent
              containerPort: 8642
              protocol: TCP
          resources:
            requests:
              memory: 64Mi
              cpu: 100m
            limits:
              memory: 2Gi
              cpu: 1500m
          volumeMounts:
            - name: hermes-agent-data
              mountPath: /opt/data
            - name: shm
              mountPath: /dev/shm
        - name: dashboard
          image: docker.io/nousresearch/hermes-agent:v2026.5.7 # {"$imagepolicy": "flux-system:hermes-agent-policy"}
          command:
            - hermes
          args:
            - dashboard
            - '--host'
            - 0.0.0.0
            - '--insecure'
          env:
            - name: HOME
              value: /opt/data
            - name: PATH
              value: /opt/hermes/.venv/bin:/opt/data/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: GATEWAY_HEALTH_URL
              value: http://hermes.hermes-agent.svc.cluster.local:8642
          ports:
            - name: dashboard
              containerPort: 9119
              protocol: TCP
          resources:
            requests:
              memory: 64Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          volumeMounts:
            - name: hermes-agent-data
              mountPath: /opt/data
        - name: code-server
          image: docker.io/linuxserver/code-server:4.118.0 # {"$imagepolicy": "flux-system:code-server-policy"}
          env:
            - name: PUID
              value: '1000'
            - name: PGID
              value: '1000'
            - name: TZ
              value: America/New_York
            - name: DEFAULT_WORKSPACE
              value: '/projects'
          ports:
            - name: https
              containerPort: 8443
              protocol: TCP
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 2Gi
              cpu: "2"
          volumeMounts:
            - name: hermes-agent-data
              mountPath: /projects
            - name: vscode-config
              mountPath: /config
      restartPolicy: Always
      volumes:
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: "2Gi"
  volumeClaimTemplates:
    - metadata:
        name: hermes-agent-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 16Gi
        storageClassName: longhorn
    - metadata:
        name: vscode-config
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 4Gi
        storageClassName: longhorn
