apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vane-data
  namespace: vane
  labels:
    app: vane
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
