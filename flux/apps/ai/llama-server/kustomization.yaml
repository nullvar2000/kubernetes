apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - imageupdateautomation.yaml

configMapGenerator:
  - name: llama-server-env
    namespace: llama-server
    envs:
      - files/llama-server.env
  - name: models-preset
    namespace: llama-server
    envs:
      - files/models-preset.ini
