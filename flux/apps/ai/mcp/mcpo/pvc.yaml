apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcpo-data
  namespace: mcp
  labels:
    app: mcpo
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
