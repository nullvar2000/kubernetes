apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - httproute.yaml
  - imageupdateautomation.yaml
  - pvc.yaml
  - service.yaml
  - statefulset.yaml

configMapGenerator:
  - name: echomuse-controller-env
    namespace: homeassistant
    envs:
      - files/echomuse-controller.env