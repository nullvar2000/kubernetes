apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kanboard
  namespace: kanboard
spec:
  parentRefs:
  - name: gateway-api
    namespace: istio-gateway
  hostnames:
    - kanboard.${GLOBAL_DOMAIN}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: kanboard
      port: 80
