---
apiVersion: v1
kind: Service
metadata:
  name:  kanboard
spec:
  selector:
    app:  kanboard
  type: ClusterIP
  ports:
    - name: web-ui
      port: 80
      protocol: TCP
      targetPort: 80
