apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - httproute.yaml
  - imageupdateautomation.yaml
  - namespace.yaml
  - service.yaml

configMapGenerator:
  - name: searxng-settings
    namespace: searxng
    files:
      - files/settings.yml
