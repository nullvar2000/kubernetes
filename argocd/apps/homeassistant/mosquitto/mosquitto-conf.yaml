apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-conf.yaml
  namespace: homeassistant
data:
  mosquitto.conf: |-
    persistence true
    persistence_location /mosquitto/data/
    log_dest file /mosquitto/log/mosquitto.log
immutable: true