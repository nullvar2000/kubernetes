alertmanager:
  ingress:
    enabled: 'true'
    ingressClassName: nginx
    hosts:
      - monitoring.legion.lan
    paths:
      - /alertmanager
    pathType: Prefix
  alertmanagerSpec:
    routePrefix: /alertmanager
grafana:
  ingress:
    enabled: 'true'
    ingressClassName: nginx
    hosts:
      - monitoring.legion.lan
    paths:
      - /grafana
    pathType: Prefix
prometheus:
  thanosIngress:
    enabled: 'true'
    ingressClassName: nginx
    hosts:
      - monitoring.legion.lan
    paths:
      - /thanos
    pathType: Prefix
  ingress:
    enabled: 'true'
    ingressClassName: nginx
    hosts:
      - monitoring.legion.lan
    paths:
      - /prometheus
    pathType: Prefix
