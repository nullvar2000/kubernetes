apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argocd-setup
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/nullvar2000/kubernetes
      revision: HEAD
      directories:
      - path: argocd/argocd-setup/*
  template:
    metadata:
      name: '{{ path.basename }}'
    spec:
      project: default
      source:
        repoURL: https://github.com/nullvar2000/kubernetes
        targetRevision: HEAD
        path: '{{ path }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ path.basename }}'