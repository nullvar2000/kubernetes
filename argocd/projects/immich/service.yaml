apiVersion: v1
kind: Service
metadata:
  name:  immich-postgresql
  namespace: immich
spec:
  selector:
    app:  postgresql
  type: ClusterIP
  ports:
    - name:  postgres
      port:  5432
      protocol: TCP 
